# The Cloudflare Worker in ./worker. Leave unset to build without a backend:
# note sharing, Telegram reminders and dictionary sync all hide themselves.
NEXT_PUBLIC_API_URL=

# GitHub Pages project sites are served from a sub-path.
NEXT_PUBLIC_BASE_PATH=
NEXT_PUBLIC_SITE_URL=
