{
  "name": "ethiotime-api",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "schema": "wrangler d1 execute ethiotime --remote --file=./schema.sql",
    "schema:local": "wrangler d1 execute ethiotime --local --file=./schema.sql",
    "test": "node --test \"src/**/*.test.ts\""
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260820.1",
    "typescript": "^5",
    "wrangler": "^4"
  }
}
